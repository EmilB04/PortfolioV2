{
  "//": "Scopes the editor's TypeScript service to the edge functions so deno.d.ts applies. Not part of the app build: tsconfig.app.json only includes src/, and this project is not referenced from the root tsconfig.json.",
  "compilerOptions": {
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": [],
    "allowJs": true,
    "skipLibCheck": true,
    "noEmit": true,
    "isolatedModules": true
  },
  "include": ["**/*.ts"]
}
